# 功能说明
> 依赖收集的操作类型

## OperationTypes
> 依赖收集的操作类型
```ts
export const enum OperationTypes {
  SET = 'set',
  ADD = 'add',
  DELETE = 'delete',
  CLEAR = 'clear',
  GET = 'get',
  HAS = 'has',
  ITERATE = 'iterate'
}
```